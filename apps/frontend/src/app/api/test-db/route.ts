import { NextResponse } from 'next/server'; import { db } from '../../../lib/auth'; import { users } from '../../../../../backend/src/db/schema'; export async function GET() { try { const allUsers = await db.select().from(users).limit(1); return NextResponse.json({ success: true, allUsers }); } catch (error: any) { return NextResponse.json({ success: false, error: error.message }, { status: 500 }); } }
