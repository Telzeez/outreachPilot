import { NextResponse } from 'next/server'; import { db } from '../../../../lib/auth'; import { sql } from 'drizzle-orm'; export async function GET() { try { await db.execute(sql\"ALTER TABLE \"users\" ADD COLUMN IF NOT EXISTS \"password\" text;\"); return NextResponse.json({ success: true, message: \"Database migrated successfully\" }); } catch (error: any) { return NextResponse.json({ error: String(error) }, { status: 500 }); } }
